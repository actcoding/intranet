APP_NAME=Intranet
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_TIMEZONE=Europe/Berlin
APP_URL=http://intranet.local

APP_LOCALE=de
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=de_DE

APP_MAINTENANCE_DRIVER=cache
APP_MAINTENANCE_STORE=redis

JWT_KEY=
JWT_TTL_ACCESS_TOKEN=1 day

LOG_CHANNEL=stack
LOG_STACK=daily,stderr
LOG_DEPRECATIONS_CHANNEL=stderr
LOG_LEVEL=warn

DB_CONNECTION=pgsql
DB_HOST=db
DB_PORT=5432
DB_DATABASE=intranet
DB_USERNAME=intranet
DB_PASSWORD=

SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

FILESYSTEM_DISK=local
BROADCAST_CONNECTION=redis
QUEUE_CONNECTION=redis

CACHE_STORE=redis

REDIS_HOST=redis
REDIS_PASSWORD=
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=
MAIL_PORT=465
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply+intranet@example.org"
MAIL_FROM_NAME="${APP_NAME}"
