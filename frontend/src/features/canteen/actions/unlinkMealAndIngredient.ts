import { linkApi } from "@/lib/api/api"

const unlinkMealAndIngredient = (request: any) => {
    console.log('unlinkMealAndIngredient', request)
    // linkApi.unlinkMealAndIngredient(request) oder so
    // linkApi.linkDetach
}

export { unlinkMealAndIngredient }